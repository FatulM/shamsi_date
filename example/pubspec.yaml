name: example
description: shamsi_date example.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  shamsi_date:
    path: ../

flutter:
  uses-material-design: true
