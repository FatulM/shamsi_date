name: example
description: An example for shamsi_date package.
version: 1.0.0+1
publish_to: 'none'

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  shamsi_date:
    path: ../

flutter:
  uses-material-design: true