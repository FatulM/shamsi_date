include: package:lints/recommended.yaml

analyzer:
  strong-mode:
    implicit-dynamic: false
    implicit-casts: false

linter:
  rules:
    # since this library is full of math calculations,
    # I think it is better to be strict about types.
    omit_local_variable_types: false
